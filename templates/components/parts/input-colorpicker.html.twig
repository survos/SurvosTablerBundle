{# templates/components/parts/input-colorpicker.html.twig v4.8 #}
<div class="mb-3">
   <label class="form-label">{{ label | default('Color picker') }}</label>
   <input type="color" class="form-control form-control-color" value="{{ color | default(site.colors.blue.hex) }}" title="Choose your color">
</div>

{#
=== Original Liquid (form/input-colorpicker.html) ===
<div class="mb-3">
   <label class="form-label">{{ include.label | default: 'Color picker' }}</label>
   <input type="color" class="form-control form-control-color" value="{{ include.color | default: site.colors.blue.hex }}" title="Choose your color">
</div>

#}